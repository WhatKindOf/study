이번에는 To-Do-List를 만들어보자.

앞서 만든 greetings 와 비슷하다.

단지 다른 점은, hiding과 showing을 가지지 않을 것이다.

이것은 단지 to-do list니까

다른 JavaScript파일이라 하더라도
const(상수)니까 변수명을 다르게 해줘야 한다.

init( )에서 가장 먼저 해줘야 할 것은
local storage에서 이미 저장되어있는 to-do list값들을 가져오는 것.


만약 이미 저장된 '할일'이 없다면
아무것도 안할 것이다.
왜냐하면 ToDo list는 항상 값을 입력받도록 대기중인 상태니까.
(form은 항상 showing 상태)

이번에도 input태그에 입력이 되고 submit되면
기본 동작은 일단 막는다.
그리고 앞서 배운 내용이랑 똑같다.
현재 input태그에 입력된 값을 가져오고
띄워주고, local storage에 저장된다.

다만 엔터를 누르고나면 
또 다시 입력을 받아야하는 상태로 되돌아가야하기 때문에
input에 있는 값을 없애준다.
toDoInput.value = "";


querySelector는 이미 존재하는 것을 가져오는 것인데,
만약 우리가 뭔가를 생성하기를 원한다면???
다음과 같이 작성하면 된다.

const li = document.createElement("li");
<li>태그를 생성하고 싶을때

그리고 li.appendChild 를 쓰는데
이는 뭔가를 그의 부모 element안에 넣는것.

li.appendChild(span); 하게 되면 <li>태그 안에 <span>을 넣는다
li.appendChild(delBtn); 해서 <btn>도 <li>태그 안에 넣어주고,
최종적으로 toDoList.appendChild("li"); 해서
<ul>태그에 <li>태그를 넣어준다.