일단 우리가 사용할 툴에 대해 먼저 알아보자.

원하는 에디터를 먼저 선정. (Visual Studio Code, Atom, Sublime Text 등)

강의에서는 Repl를 사용할 거다.
Code Editor 같은 건데 다운로드가 필요없다.
개인 파일이랑 결과를 전부 볼 수 있다.

큰 프로젝트를 할 때는 VSCode를 사용하는데,
컨셉을 보여준다거나 하는 용도의 작은 프로젝트는
그냥 Repl만 사용할거다.
https://repl.it/

먼저 JavaScript를 어떻게 페이지에 추가하는지 알아야함.

먼저 repl에 접속해서 html, css, js 를 만든다.

1. index.html에 기본적으로 적힌 내용을 일단 지운다.
2. 평범한 HTML을 만들거다.
3. 잘 작동되는지 확인완료
4. 이제 JS파일을 추가해본다.

JS파일은 항상 <body>에서 가장 아래에 있어야 함. 
<link>는 </link> 없이 그냥 <link/> 이렇게 해주면 되는데
<script>는 </script>가 필요하다.

그럼 간단한 경고 메세지를 만들어보자.
이는 가장 위에 뜨면서 아래 다른 웹 창의 접근은 우선 막는다.

그럼 이번엔 console.log를 써보자
console에서 출력물을 확인할 수 있다.


모든 웹 브라우저에는 JavaScript가 설치되어 있다.

예를 들어 어떤 소프트웨어 제품을 만들 때 Haskell이라는 언어를 사용했다고 하면
이를 열어야할 때 경우에 따라 Haskell을 설치해야 하는 경우가 있다. 이는 불편.

하지만 JavaScript는 아무도 다운로드를 하지 않아도 된다.
브라우저에 이미 같이 구성이 되어있기 때문.


