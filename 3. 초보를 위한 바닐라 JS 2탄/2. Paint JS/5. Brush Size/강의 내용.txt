이번 시간에는 브러쉬 크기를 변경해보도록 하자.

range 이벤트는 input에 반응할거다.
(<input>태그로 이루어졌으니)

이는 노마드도 여러 실수를 하면서 알아낸 점이다.
그러니 만약 당신도 실수를 엄청하게 된다면,
구글 검색을 추천한다.


range를 조절하면서 
console.log(event)를 통해 정보들을 보게 되면
우리가 원하는 정보는
event.target.value라는 것을 알 수 있다.


이번에는 context의 lineWidth 값을 덮어써서
선의 굵기를 조절할 것이다.



다음으로 해볼 것은 Fill (색상 채우기).

Fill 버튼을 누르고 색상을 누른 뒤 캔버스를 클릭하면
캔버스가 해당 색상으로 채워지는 것을 보고싶다.

Fill 모드인지 Paint 모드인지 구분할 수 있는 변수를 생성해놓는다.
그리고 버튼을 클릭할 때마다 
현재 변수에 저장된 값이 true인지 false인지 구분해서
모드 버튼에 Fill이나 Paint 글자로 변경하도록 한다.